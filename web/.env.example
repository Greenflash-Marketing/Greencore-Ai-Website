# Vorlage für .env.local: Datei kopieren, in .env.local umbenennen und Werte eintragen.
# Diese Vorlage wird committet – hier NIE echte Geheimnisse (Tokens, Passwörter) eintragen.

# Sanity – öffentliche Werte (NEXT_PUBLIC_ landet auch im Browser-Code)
NEXT_PUBLIC_SANITY_PROJECT_ID=9xpdwcge
NEXT_PUBLIC_SANITY_DATASET=production

# 21st.dev – Komponenten-Registry (Schluessel aus dem 21st-Konto, nur lokal eintragen)
API_KEY_21ST=
